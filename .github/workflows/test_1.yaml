name: Hola mundo

on: [push]

jobs:
    hola: #job se ejecuta dentro de ubuntu actual
        runs-on: ubuntu-latest

        steps:
            -   name: hola-step
                run: echo "Hola mundo"

    other: 
        runs-on: ubuntu-latest 

        steps:
            -   name: LS
                run: ls -al
